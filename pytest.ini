[pytest]
markers =
    integration: end-to-end tests that require a live Kafka broker (docker compose up)

# Run only unit tests by default; use -m integration to run the full pipeline test.
addopts = -m "not integration"
